#include "main.h"

/**
 *_abs - Entry
 *
 * @n: int type number
 *
 * Return: absolute value of @n
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		n *= -1

		return (n);
	}

	return (0);
}

