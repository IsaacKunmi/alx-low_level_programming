#include "main.h"

/**
 *_strlen - find length of string
 *
 * @s: char type
 *
 * Return: integer
 */

int _strlen(char *s)
{
	 int i, x = 0;

	 for (i = 0; str[i] != '\0'; i++)
	 {
		 x++
	 }

	 return(x);

}
